# Secrets and local env
autoagents/.env
.env
*.env.local

# Autoagents runtime (regenerated each loop)
autoagents/.runtime/
autoagents/var/loop/

# Docker / mail data (when deployed)
mail-data/
*.pem
*.key

# OS / editor
.DS_Store
*.swp
