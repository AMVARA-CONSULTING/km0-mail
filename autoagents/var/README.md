Runtime artifacts for the autoagents loop (preflight digests, gh stderr captures).

Default path: `autoagents/var/loop/` (override with `AGENT_LOOP_TMP` in `autoagents/.env`).

Not committed except this README.
